The User "admin" (with password "admin") to get to the "teacher page" which has power to delete users from the database as well as 
print an HTML file with a table of all users' progress and password.
In order to access the teacher html report simply open up the application folder and you will see an icon of your default web browser 
called TeacherReport.html. 